name: V4 Stage Capsule Canary

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  canary:
    uses: kungfu-systems/buildchain/.github/workflows/v4-stage-capsule-canary.yml@f4f2471269521e56a6f1c2a743f8244dc2a9a557
    with:
      consumer: agent-hub-demo
      node-version: "24"
      install-artifact-path: node_modules/.package-lock.json
      build-artifact-path: dist/agent-hub-demo.json
      verify-artifact-path: dist/agent-hub-demo.json
