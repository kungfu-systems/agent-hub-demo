name: V4 Stage Capsule Canary

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  canary:
    uses: kungfu-systems/buildchain/.github/workflows/v4-stage-capsule-canary.yml@47bb098a7701e55fcd1276e795acc170574d9744
    with:
      consumer: agent-hub-demo
      node-version: "24"
      install-artifact-path: node_modules/.package-lock.json
      build-artifact-path: dist/agent-hub-demo.json
      verify-artifact-path: dist/agent-hub-demo.json
