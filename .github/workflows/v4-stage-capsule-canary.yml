name: V4 Stage Capsule Canary

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  canary:
    uses: kungfu-systems/buildchain/.github/workflows/v4-stage-capsule-canary.yml@0a0cb4e40b9d97b1353ee48c77b29dc58049d2fc
    with:
      consumer: agent-hub-demo
      node-version: "24"
      install-artifact-path: node_modules/.package-lock.json
      build-artifact-path: dist/agent-hub-demo.json
      verify-artifact-path: dist/agent-hub-demo.json
